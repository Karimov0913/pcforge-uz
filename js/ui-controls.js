/* Persistent day/night theme and circular scroll progress. */
(()=>{const body=document.body,themeButton=document.querySelector('#themeBtn');const saved=localStorage.getItem('pcforge-theme');const setTheme=night=>{body.classList.toggle('night',night);body.classList.remove('light');document.documentElement.style.colorScheme=night?'dark':'light';localStorage.setItem('pcforge-theme',night?'night':'day');if(themeButton){themeButton.textContent=night?'☀':'☾';themeButton.title=night?'Включить дневную тему':'Включить ночную тему';themeButton.setAttribute('aria-label',themeButton.title);themeButton.setAttribute('aria-pressed',String(night))}};setTheme(saved?saved==='night':false);themeButton?.addEventListener('click',e=>{e.preventDefault();e.stopImmediatePropagation();setTheme(!body.classList.contains('night'))},{capture:true});const button=document.createElement('button');button.className='scroll-progress';button.type='button';button.setAttribute('aria-label','Наверх');button.title='Наверх';button.innerHTML='<svg viewBox="0 0 48 48" aria-hidden="true"><circle class="scroll-track" cx="24" cy="24" r="21"/><circle class="scroll-value" cx="24" cy="24" r="21" pathLength="100"/></svg><span>↑</span>';document.body.append(button);button.addEventListener('click',()=>window.scrollTo({top:0,behavior:'smooth'}));let queued=false;const update=()=>{queued=false;const max=Math.max(1,document.documentElement.scrollHeight-innerHeight),progress=Math.min(100,Math.max(0,scrollY/max*100));button.style.setProperty('--progress',progress);button.classList.toggle('visible',scrollY>280);button.setAttribute('aria-label',`Наверх, просмотрено ${Math.round(progress)}%`)};addEventListener('scroll',()=>{if(!queued){queued=true;requestAnimationFrame(update)}},{passive:true});addEventListener('resize',update,{passive:true});update()})();
